
paths:

  rig:
    pattern: '/jobs/{job.code}/assets/model/{lod}'
  model:
    pattern: '/jobs/{job.code}/assets/rig/{rig_type}'
  model_sandbox:
    pattern: '/jobs/{job.code}/assets/model/{lod}/sandbox'